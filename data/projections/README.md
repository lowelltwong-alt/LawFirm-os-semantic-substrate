# Property Graph Projection Templates

This directory contains additive projection contracts for bounded semantic graph
packages.

Projection artifacts should remain derivative and machine-readable. They should
not replace the canonical claim, artifact, or substrate records from which they
are built.
