# Release Snapshots

This directory is reserved for versioned machine-readable release snapshots.

## Intended outputs

- `graph.ttl`
- `graph.jsonld`
- `nodes.ndjson`
- release manifest metadata

## Rule

Human-readable governance notes may explain the system, but machine-readable snapshots should become the durable release surface.
