# Semantic Context Templates

This directory holds additive JSON-LD context templates and pilot artifacts for
the semantic graph stack.

These context files do not replace the repository's current canonical schemas.
They prepare bounded graph-facing exports while preserving the existing
validation-first shell.
