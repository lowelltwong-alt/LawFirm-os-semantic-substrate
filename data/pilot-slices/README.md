# Pilot Slice Definitions

This directory holds bounded pilot slice definitions.

Pilot slices should remain infrastructure-proving and deliberately small in
scope during this phase.

Semantic-stack pilot slices may reference additive JSON-LD contexts, named
graph manifests, property-graph projections, and retrieval sidecars so long as
those artifacts remain bounded and do not replace the canonical validation
surface.
