# Templates

This directory is reserved for fill-in artifact templates.

## Purpose

Templates are example authoring surfaces. They are not the ontology itself and they are not the JSON Schema validation surface.

## Rule

- `templates/` holds fill-in artifacts or starter objects
- `schemas/` holds JSON Schema validators
- `ontology/` holds canonical semantic assets
- `shapes/` holds SHACL validation assets
